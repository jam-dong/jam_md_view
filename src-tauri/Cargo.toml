[package]
name = "jam_md_view"
version = "0.1.0"
description = "A lightweight Tauri-based Markdown editor and viewer"
authors = ["jam"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "jam_md_view_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-single-instance = "2"
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = false
strip = true
codegen-units = 1
